<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_margin="10dp"
              android:layout_width="match_parent"
              android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_full_leftarrow"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:layout_marginRight="20dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginEnd="20dp"
        android:layout_gravity="center"
        app:srcCompat="@drawable/ic_left_arrow"/>

    <TextView
            android:id="@+id/tv_full_actionbartitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/mobss_color_white"
            android:layout_marginLeft="10dp"
            app:layout_constraintLeft_toRightOf="@+id/iv_full_leftarrow"
            app:layout_constraintRight_toLeftOf="@+id/iv_full_favicon"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="@string/app_name"
            android:textSize="20sp"/>

    <com.varunest.sparkbutton.SparkButton
            android:id="@+id/iv_full_favicon"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_gravity="center"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:sparkbutton_activeImage="@drawable/ic_favoried"
            app:sparkbutton_inActiveImage="@drawable/ic_favory"
            app:sparkbutton_iconSize="30dp"
            app:sparkbutton_primaryColor="@color/mobss_color_white"
            app:sparkbutton_secondaryColor="@color/mobss_color_light_gray"/>
</android.support.constraint.ConstraintLayout>